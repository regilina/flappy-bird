// Constants related to game states
export const GAME_STATES = {
  GET_READY: 0,
  GAME: 1,
  OVER: 2
}

// Constants related to sprite image paths
export const SPRITE_PATHS = {
  SPRITE: 'img/sprite.png'
}

// Constants related to audio file paths
export const AUDIO_PATHS = {
  SCORE: 'audio/sfx_point.wav',
  FLAP: 'audio/sfx_flap.wav',
  HIT: 'audio/sfx_hit.wav',
  SWOOSHING: 'audio/sfx_swooshing.wav',
  DIE: 'audio/sfx_die.wav'
}

// Other game-related constants
export const GAME_CONSTANTS = {
  BIRD_JUMP: 4.6,
  BIRD_GRAVITY: 0.25,
  BIRD_ROTATION_DEGREE: 90,
  PIPE_GAP: 130,
  PIPE_SPEED: 2
}

// Font styles
export const FONT_STYLES = {
  SCORE_FONT: '35px Teko',
  GAME_OVER_FONT: '25px Teko'
}
